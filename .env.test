# Test Environment Configuration
ENV=test
PORT=8000
HOST=localhost

# GitHub Configuration (test values)
GITHUB_TOKEN=test_github_token
GITHUB_WEBHOOK_SECRET=test_webhook_secret
GITHUB_APP_ID=12345
GITHUB_APP_PRIVATE_KEY=test_private_key
GITHUB_OWNER=test_owner
GITHUB_REPO=test_repo

# AI Provider Configuration (test values)
OPENAI_API_KEY=test_openai_key
OPENAI_MODEL=gpt-4-turbo-preview
OPENAI_MAX_TOKENS=4096
OPENAI_TEMPERATURE=0.7

ANTHROPIC_API_KEY=test_anthropic_key
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022
ANTHROPIC_MAX_TOKENS=4096
ANTHROPIC_TEMPERATURE=0.7

AI_PROVIDER=openai

# Database Configuration (test values)
SQLITE_PATH=:memory:
SQLITE_WAL_MODE=false

# Redis (disabled for tests)
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=
REDIS_DB=1
REDIS_RETRY_ATTEMPTS=1
REDIS_RETRY_DELAY=100

# Job Queue Configuration (minimal for tests)
QUEUE_NAME=test-tasks
QUEUE_MAX_JOBS=100
QUEUE_CONCURRENCY=1
QUEUE_RETRY_ATTEMPTS=1
QUEUE_RETRY_DELAY=100
QUEUE_JOB_TIMEOUT=30000

# Worker Configuration (minimal for tests)
WORKER_POOL_SIZE=1
WORKER_MAX_JOBS_PER_WORKER=5

# Rate Limiting (disabled for tests)
RATE_LIMIT_WINDOW=60000
RATE_LIMIT_MAX_REQUESTS=1000
RATE_LIMIT_ENABLED=false

# Security (minimal for tests)
WEBHOOK_SIGNATURE_ALGORITHM=sha256
API_KEY_REQUIRED=false
API_KEY=test_api_key
CORS_ORIGINS=http://localhost:3000
ENABLE_HELMET=false

# Logging Configuration (minimal for tests)
LOG_LEVEL=error
LOG_FILE_PATH=./logs/test.log
LOG_MAX_SIZE=1048576
LOG_MAX_FILES=1
LOG_ROTATION=none

# Metrics & Analytics (disabled for tests)
METRICS_ENABLED=false
METRICS_PORT=9091
METRICS_PATH=/metrics

# AI Cost Tracking (disabled for tests)
COST_TRACKING_ENABLED=false
COST_ALERT_THRESHOLD=100.00
COST_BUDGET_MONTHLY=1000.00

# AI Response Caching (disabled for tests)
AI_CACHE_ENABLED=false
AI_CACHE_TTL=60
AI_CACHE_MAX_SIZE=10

# Workflow Configuration (fast for tests)
WORKFLOW_TIMEOUT=30000
WORKFLOW_MAX_RETRIES=1
WORKFLOW_AUTO_MERGE=false
WORKFLOW_REQUIRE_TESTS=false

# Dashboard Configuration (disabled for tests)
DASHBOARD_ENABLED=false
DASHBOARD_PORT=3001
DASHBOARD_AUTH_ENABLED=false
DASHBOARD_USERNAME=test
DASHBOARD_PASSWORD=test

# Notification Configuration (disabled for tests)
SLACK_WEBHOOK_URL=
DISCORD_WEBHOOK_URL=
EMAIL_ENABLED=false
EMAIL_SMTP_HOST=smtp.test.com
EMAIL_SMTP_PORT=587
EMAIL_FROM=test@test.com
EMAIL_TO=test@test.com

# Performance Tuning (minimal for tests)
MAX_CONCURRENT_REQUESTS=10
REQUEST_TIMEOUT=5000
BODY_PARSER_LIMIT=1mb

# Development (test mode)
DEBUG=false
VERBOSE_LOGGING=false
DRY_RUN=true
